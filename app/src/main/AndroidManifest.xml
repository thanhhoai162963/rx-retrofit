<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.retrofit_rx_recyclerview">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Retrofit_Rx_Recyclerview">
        <activity
            android:name=".view.detail.DetailActivity"
            android:label="@string/title_activity_detail"
            android:theme="@style/Theme.Retrofit_Rx_Recyclerview.NoActionBar">

        </activity>
        <activity
            android:name=".view.comment.CommentsActivity"
            android:label="CommentsActivity"
            android:theme="@style/Theme.Retrofit_Rx_Recyclerview.NoActionBar">

        </activity>
        <activity
            android:name="com.example.retrofit_rx_recyclerview.view.articles.ArticlesActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>